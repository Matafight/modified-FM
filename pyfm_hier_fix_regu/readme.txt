python setup.py build_ext --inplace

1.FM with hier, the tuning parameters in this setting is fixed and cross-validation hasn't been used here
2. we share the same regularized params in all factorized vectors and all main effects
